import { Edit2 } from './edit2';

export default function Editor({ handleCompileCode }) {
    return (
        <div>
            <Edit2 handleCompileCode={handleCompileCode} />
        </div>
    );
}
